import React from 'react';

const Player = () => {
    return (
        <div className="footer" >
            <i className="far fa-play-circle"
                style={{
                    fontSize: `3em`,
                    color: `deeppink`,
                    cursor: `pointer`
                }}
            ></i>
        </div>
    );
};

export default Player;
